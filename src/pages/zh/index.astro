---
import BaseLayout from '@/layouts/BaseLayout.astro';
import SnailGlyph from '@/components/SnailGlyph.astro';
import Asterism from '@/components/Asterism.astro';
import SectionMark from '@/components/SectionMark.astro';

const title = 'HiQ Cortex — LCA 工作台，每一行都经得起追问';
const description =
  '跨 12 个 LCA 数据库匹配排放因子，逐行 DQI 评分，在三道人工确认闸前停住。每一份导出，都能说清来路。';

const audiences = [
  {
    label: '面向 LCA 顾问和审核员',
    body:
      '在数据库标签页之间往返的时间压缩成一次对话。Cortex 跨 12 个数据库并行检索，逐行返回 DQI 评分候选项，在审核员之前替你标出缺口。方法论问答引用的是数据集记录本身，不是训练数据的摘要。',
    href: '/zh/solutions/bom-matching',
    cta: '查看 BOM 匹配',
  },
  {
    label: '面向可持续发展与 CBAM 团队',
    body:
      'CBAM 申报要求嵌入式排放证据，而供应商往往还没提交。Cortex 准备《附件 IV》所需的排放因子和代理值论证材料，支持 GHG Protocol 全部 15 个范围三类别，并保留系统模型元数据——让申报内容读起来是准确的，而不是近似的。',
    href: '/zh/standards',
    cta: '查看标准对齐',
  },
  {
    label: '面向基于 LCA 数据开发的团队',
    body:
      'Cortex Chat 和 Cortex Cowork 背后的推理引擎，通过四种协议开放：REST、MCP、AG-UI、A2A。12 个数据库的接入面、DQI 评分、三道人工确认闸——一把 API key 全部接入。把 LCA 检索接进你自己的产品，不用自己维护数据库。',
    href: '/zh/docs',
    cta: '阅读 API 文档',
  },
];

const products = [
  {
    name: 'Cortex Chat',
    label: 'CORTEX CHAT · WEB',
    body:
      '带来物料名称、一行 BOM，或一个标准问题。Cortex 先问一个澄清问题——合金牌号、生产路线、地区——然后跨 12 个数据库检索，返回附带 GWP100 和五维 DQI 评分的候选数据集。不需要配置表格，不需要数据库许可证才能发问。',
    proof: '物料查询 · 约 2 分钟 · 附溯源的数据集排名',
    primary: { href: '/chat', label: '打开 Cortex Chat' },
    secondary: { href: '/zh/product/chat', label: '产品详情' },
  },
  {
    name: 'Cortex Cowork',
    label: 'CORTEX COWORK · DESKTOP',
    body:
      'LCA 项目跨越数周。CBAM 申报有完整的文件轨迹。Cowork 跨会话保留项目状态——项目工作区、决策日志、引用历史、六层记忆，让周一的代理值替换在周五仍然可查。从本地磁盘读取文件，运行本地工具，在一次对话中驱动 openLCA。',
    proof: '多会话项目 · 完整溯源 · 可导出的推理链',
    primary: { href: 'https://github.com/HiQ-AI/cortex-desktop/releases/latest', label: '下载 Cortex Desktop' },
    secondary: { href: '/zh/product/cowork', label: '产品详情' },
  },
];

const claims = [
  {
    stat: '跨 12 个数据库并行检索',
    body: 'HiQLCD、Ecoinvent、EF（环境足迹）、CarbonMinds、天工、World Steel、HiQLCD-AL，以及其他数据库。每条候选项返回 GWP100 值、单位、地理区域和系统模型。受限（需授权）的数据集返回标记，GWP 值在获得访问权限前不展示。返回的是 top-k，不是 top-1。从业者看到的是数据的分布，不是单一答案。',
  },
  {
    stat: 'DQI 五维评分',
    body: '时间性、地理性、技术性、完整性、可靠性——即 Pedigree Matrix 在 EF 方法中的操作化。Cortex 归一化到 0–1，分数越高越好。这不是置信区间；这是五个具名维度，从业者可以独立审查每一个。',
  },
  {
    stat: '三道人工确认闸',
    body: '覆盖率低于 80%。需要代理值替换。跨库 GWP 差异超过 2×。每道闸触发时，Cortex 暂停，呈现选择，等待。自动化在"沉默的答案会破坏审计"的那一刻停下来。由从业者决定；Cortex 把决定记入推理链。',
  },
];
---

<BaseLayout title={title} description={description} locale="zh">

  <!-- Hero -->
  <section class="relative overflow-hidden">
    <div class="container-page pt-14 md:pt-20 pb-24 md:pb-32">

      <div class="rise rise-1 mb-10 flex items-baseline justify-between border-b border-rule pb-4">
        <p class="eyebrow">HiQ Cortex · LCA 工作台 · HiQ-AI 出品</p>
        <p class="hidden sm:block" style="font-family: 'Newsreader Variable', serif; font-variation-settings: 'opsz' 14; font-style: italic; color: var(--ink-faint); font-size: 0.86rem;">
          卷 01 · 关于慢答案
        </p>
        <p class="running-footer">{new Date().toISOString().slice(0, 10)}</p>
      </div>

      <div class="grid gap-10 md:grid-cols-[1.25fr_1fr] md:gap-14 items-start">

        <div class="relative">
          <h1 class="rise rise-2 font-display" style="font-size: clamp(2.4rem, 5.5vw, 4.6rem); line-height: 1.15; letter-spacing: -0.01em; font-variation-settings: 'SOFT' 30, 'opsz' 144; font-weight: 400;">
            <em style="color: var(--shell); font-style: italic; font-variation-settings: 'SOFT' 80;">LCA 工作台</em>，<br/>
            每一行都<br/>
            <span style="color: var(--ink-faint);">经得起追问。</span>
          </h1>

          <p class="rise rise-3 mt-10 max-w-xl font-serif italic" style="font-size: 1.12rem; line-height: 1.85; font-variation-settings: 'opsz' 20; color: var(--ink);">
            跨 <strong style="color: var(--ink); font-weight: 500; font-style: normal;">12 个 LCA 数据库</strong>匹配排放因子，逐行 DQI 评分，在三道人工确认闸前停住。每一份导出，都能说清来路。
          </p>

          <div class="rise rise-5 mt-12 flex flex-wrap items-center gap-4">
            <a href="/zh/solutions/bom-matching" class="ticket">查看 BOM 匹配流程 <span aria-hidden="true">→</span></a>
            <a href="/chat" class="ticket-ghost">试用 Cortex Chat</a>
          </div>
        </div>

        <aside class="relative rise rise-3 md:pt-6">
          <div class="relative flex justify-center md:justify-end" style="color: var(--ink);">
            <SnailGlyph class="w-full max-w-md" />
          </div>
          <div class="margin-note mt-6 md:mt-10 grid gap-4 sm:grid-cols-2">
            <div>
              <span class="eyebrow block mb-2">图一</span>
              <em>Cornu aspersum</em>：一座随身携带的房子。壳里是数据来源，痕迹里是推理过程。
            </div>
            <div>
              <span class="eyebrow block mb-2">图二</span>
              12 个数据库。DQI 五维。三道确认闸。一次对话。
            </div>
          </div>
        </aside>
      </div>
    </div>
  </section>

  <!-- §II 三扇门 -->
  <section class="border-t border-rule">
    <div class="container-page py-20 md:py-24">
      <SectionMark kicker="三扇门" numeral="II" title="工作内容不同，底层引擎相同。选那扇属于你的门。" />

      <div class="mt-12 grid gap-px overflow-hidden border border-rule md:grid-cols-3" style="background: var(--rule);">
        {audiences.map((a, i) => (
          <article class="p-8 md:p-10 flex flex-col gap-5" style="background: var(--paper);">
            <span class="font-display italic" style="color: var(--shell); font-variation-settings: 'SOFT' 40; font-size: 1.1rem;">§ {String(i + 1).padStart(2, '0')}</span>
            <h3 class="font-display" style="font-variation-settings: 'SOFT' 40, 'opsz' 40; font-size: 1.35rem; line-height: 1.3; letter-spacing: -0.005em;">{a.label}</h3>
            <p class="font-serif" style="line-height: 1.85; font-variation-settings: 'opsz' 17; color: var(--ink); font-size: 0.96rem;">{a.body}</p>
            <a href={a.href} class="mt-auto smallcaps" style="text-decoration: none; color: var(--shell);">{a.cta} →</a>
          </article>
        ))}
      </div>
    </div>
  </section>

  <!-- §III 两款产品 -->
  <section class="border-t border-rule" style="background: var(--paper-rim);">
    <div class="container-page py-20 md:py-24">
      <SectionMark kicker="同一引擎，两个界面" numeral="III" title="Chat 答问题；Cowork 跑流程。" />

      <div class="mt-14 grid gap-px overflow-hidden border border-rule md:grid-cols-2" style="background: var(--rule);">
        {products.map((p) => (
          <article class="p-10 md:p-12 flex flex-col gap-5" style="background: var(--paper);">
            <p class="eyebrow" style="color: var(--shell);">{p.label}</p>
            <h3 class="font-display" style="font-variation-settings: 'SOFT' 40, 'opsz' 60; font-size: clamp(1.7rem, 2.8vw, 2.3rem); line-height: 1.1; letter-spacing: -0.015em;">{p.name}</h3>
            <p class="font-serif" style="line-height: 1.85; font-variation-settings: 'opsz' 18; color: var(--ink); font-size: 1rem;">{p.body}</p>
            <p class="font-mono" style="font-size: 0.72rem; letter-spacing: 0.06em; color: var(--ink-faint);">{p.proof}</p>
            <div class="mt-auto flex flex-wrap items-center gap-4 pt-4">
              <a href={p.primary.href} class="ticket">{p.primary.label} <span aria-hidden="true">→</span></a>
              <a href={p.secondary.href} class="smallcaps" style="text-decoration: none; color: var(--ink-mute);">{p.secondary.label} →</a>
            </div>
          </article>
        ))}
      </div>
    </div>
  </section>

  <!-- §IV 为什么是蜗牛 -->
  <section class="border-t border-rule">
    <div class="container-page py-20 md:py-28">
      <SectionMark kicker="为什么是蜗牛" numeral="IV" title="刻意选了火箭的反面。" />

      <p class="max-w-3xl font-serif" style="font-size: 1.08rem; line-height: 1.85; font-variation-settings: 'opsz' 20; color: var(--ink);">
        一份 CBAM 申报或 EPD 背后的每一个数字，都要经得住读过 ISO 14044 的审计员的审查——他们专门在找那一行站不住的数据。Cortex 在后端做的是最快的事——12 个数据库并行检索、并行工具调用、大规模 LLM 推理。但它交付的始终是最慢的那件：一条能被逐条复核的推理链。
      </p>

      <Asterism />

      <blockquote class="mx-auto max-w-3xl text-center">
        <p class="pullquote">
          慢，是 <em style="color: var(--shell); font-style: italic;">Cortex</em> 最重要的承诺。
        </p>
        <footer class="eyebrow mt-6"><a href="/zh/about" style="color: var(--shell);">为什么选蜗牛 →</a></footer>
      </blockquote>
    </div>
  </section>

  <!-- §V 三条声明 -->
  <section class="border-t border-rule" style="background: var(--paper-rim);">
    <div class="container-page py-20 md:py-24">
      <SectionMark kicker="壳里装着什么" numeral="V" title="三条声明，每一条都能从 Cortex 的工作方式里验证。" />

      <div class="mt-12 border-y border-rule">
        {claims.map((c, i) => (
          <article class="grid items-baseline gap-6 py-10 border-b border-rule last:border-b-0 md:grid-cols-[5rem_minmax(0,_1fr)_minmax(0,_1.4fr)] md:gap-12">
            <span class="font-display italic" style="color: var(--shell); font-variation-settings: 'SOFT' 40; font-size: 1.6rem;">§ {String(i + 1).padStart(2, '0')}</span>
            <div class="font-display" style="font-variation-settings: 'SOFT' 40, 'opsz' 40; font-size: clamp(1.2rem, 2.4vw, 1.7rem); line-height: 1.2; letter-spacing: -0.01em;">{c.stat}</div>
            <p class="font-serif" style="line-height: 1.85; font-variation-settings: 'opsz' 18; color: var(--ink-mute); font-size: 0.96rem;">{c.body}</p>
          </article>
        ))}
      </div>
    </div>
  </section>

  <!-- §VI 收尾 -->
  <section class="border-t border-rule">
    <div class="container-prose py-24 text-center">
      <Asterism class="mb-10" />
      <h2 class="font-display" style="font-variation-settings: 'SOFT' 40, 'opsz' 96; font-size: clamp(2rem, 5vw, 3.5rem); line-height: 1.15; letter-spacing: -0.01em;">
        从一个问题开始。
      </h2>
      <p class="mx-auto mt-8 max-w-xl font-serif italic" style="font-size: 1.1rem; line-height: 1.75; font-variation-settings: 'opsz' 20; color: var(--ink-mute);">
        粘贴一个物料名称。上传一份 BOM。询问那个排放因子对中国供应商是否适用。Cortex 给出答案——并展示推导过程。不需要配置表格。发问不需要数据库许可证。
      </p>
      <div class="mt-10 flex flex-wrap justify-center gap-4">
        <a href="/chat" class="ticket">试用 Cortex Chat <span aria-hidden="true">→</span></a>
        <a href="/zh/standards" class="ticket-ghost">查看标准对齐</a>
      </div>
      <p class="mt-6 font-mono" style="font-size: 0.78rem; letter-spacing: 0.06em; color: var(--ink-faint);">
        <a href="mailto:info@hiqlcd.com" style="color: var(--ink-faint); text-decoration: none;">info@hiqlcd.com</a>
      </p>
    </div>
  </section>

</BaseLayout>
