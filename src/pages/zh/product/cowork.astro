---
import BaseLayout from '@/layouts/BaseLayout.astro';
import DownloadButtons from '@/components/DownloadButtons.astro';
import Asterism from '@/components/Asterism.astro';
import SectionMark from '@/components/SectionMark.astro';

const title = 'Cortex Cowork — HiQ Cortex';
const description =
  '你的文件。你的机器。你的审计记录。Cowork 是专为持续数周的 LCA 项目设计的桌面端 Agent——读取 BOM、驱动 openLCA、把每一个决策写入可版本管理的审计日志。';

const platforms = [
  { name: 'macOS',   detail: '.dmg — Apple Silicon（arm64）', note: 'Gatekeeper：首次启动若弹出来自身份不明的开发者提示，前往「系统设置 → 隐私与安全性 → 仍要打开」。' },
  { name: 'Windows', detail: '.exe 安装包',                    note: '需要 Git for Windows。应用首次启动时自动检测系统 Git。' },
];

const scenarios = [
  {
    numeral: 'I',
    label: 'BOM 清单处理',
    before: '打开 Excel。逐库查询。手动记录结果。在多个标签页之间来回核对。',
    after:  '上传 BOM。Cowork 从磁盘直接读取文件，对每一行跨 14 个 LCA 数据库并行检索，标记无可信匹配的行，将结果写回新 Excel 工作簿——每行附来源。',
    detail: '结果表中每行对应 BOM 的一种物料：匹配到的数据集名称（超链接至 hiqlcd.com 对应记录）、单位、地区、已获授权时的 GWP100 值、未获授权时显示 Licensed 占位符，以及使用替代值时的说明注释。',
    proof:  'BOM.xlsx → 并行检索 → 每行：GWP100 · 来源 · 替代值注释',
  },
  {
    numeral: 'II',
    label: 'Scope 3 计算',
    before: '查阅 GHG Protocol 文件。手动归类。分别收集排放因子。',
    after:  '提供活动数据文件。Cowork 对全部 15 个 GHG Protocol Scope 3 类别进行分类，建立数据收集清单，并以 14 个数据库的排放因子运行计算。',
    detail: '输出是含分类合计与因子来源的计算文件。类别 1（采购商品与服务）在 BOM 行级别完成匹配。',
    proof:  '活动数据输入 → 15 类别 · 因子匹配 · 计算结果输出',
  },
  {
    numeral: 'III',
    label: 'openLCA 操作',
    before: '在 openLCA 中手动建立流程。逐一匹配背景数据集。逐步翻查界面。',
    after:  '用自然语言描述目标。Cowork 匹配背景数据集、构建产品系统、解决循环引用、运行计算——在用户本地的 openLCA 实例中完成。',
    detail: 'Cowork 不替代 openLCA，而是驱动它。用户的 openLCA 实例是模型，Cowork 是操作者。启用 Computer Use（仅限 macOS）后，Cowork 可直接与 openLCA 的 GUI 交互——点击对话框、读取结果窗口、粘贴数值——无需 openLCA 的命令行接口。',
    proof:  'openLCA → 数据集匹配 → 产品系统构建 → 结果导出',
  },
  {
    numeral: 'IV',
    label: 'EPD 数据准备',
    before: '手动从建模工具拉取 LCIA 指标值。单独整理方法论文档。再次录入 EPD 模板。',
    after:  'Cowork 从预计算矩阵服务查询 GWP100 和 EN 15804+A2 多指标结果，生成含匹配数据集与替代决策的方法论摘要，输出从业者导入 EPD 工具的 Markdown 文档。',
    detail: '产品类别规则（PCR）的选择、第三方核查和正式 EPD 提交仍是从业者的步骤。Cowork 提供数据层和方法论文档，不是符合规范的 EPD 文件。',
    proof:  'LCIA 指标提取 · 方法论记录 · 从业者组装 EPD',
  },
];

const scheduleShapes = [
  {
    label: '独立型',
    body: '不归属任何项目的定时任务。适合跨项目的循环工作：每周一早上 9 点扫一遍 EF 3.1 的发布源，把 diff 邮件发出。挂在用户层级。任何项目被删都不会带走它。',
  },
  {
    label: '项目绑定型',
    body: '绑到具体某个项目的定时任务。继承项目的六层记忆、Cortex.md 指令、wiki。项目删除时一并消失。用于：每周追 EPD 数据收集、每月把 progress.md 导成 .docx 报告发客户。',
  },
  {
    label: '任务绑定型',
    body: '绑到项目内某一个任务上的定时任务。绑定是双向的：从任务能看到它的定时任务和运行历史；从定时任务能跳回触发它的那个任务。任务标记完成，定时任务即终止。适合项目生命周期内的循环子工作——EPD 发布之前，每周五重新核对供应商的 BOM。',
  },
];

const memoryLayers = [
  ['L1 用户档案',        '角色、偏好数据库（HiQLCD、Ecoinvent）、偏好影响方法、报告语言、估算策略', '用户通过设置配置'],
  ['L2 全局指令',        '适用于所有项目的规则（"始终使用 cut-off 系统模型"、"输出中文"）',         '用户通过设置配置'],
  ['L3 自动观察',        '尚未经用户确认的推断偏好',                                                '后台静默观察'],
  ['L4 Cortex.md',      '本项目的功能单位、系统边界、方法论选择。随项目文件夹一起走。',             '用户 + Agent'],
  ['L5 progress.md',    '只追加的审计记录：每个决策、数据缺口、假设、核查、质疑、里程碑。',         '仅限 Agent 通过追加专用工具写入'],
  ['L6 项目记忆',        'AI 维护的知识文件：已核查的数据集 UUID、电网因子注释、供应商专属数据。',  'Agent'],
];

const compare = [
  ['会话历史',        '云端同步',                      '仅本地'],
  ['文件访问',        '无本地文件',                    '完全读写'],
  ['脚本执行',        '不支持',                        '本地执行'],
  ['LCA 目录检索',    '服务端',                        '本地文件系统，支持离线'],
  ['技能系统',        '核心检索技能',                  '完整市场技能 + 自定义 + 观察沉淀'],
  ['审计记录',        '对话历史',                      'L5 progress.md，只追加，可版本管理'],
  ['手机派活',        '不支持',                        'Cortex Dispatch（dispatch.hiq.earth）'],
  ['Computer Use',    '不支持',                        '21 个工具，仅限 macOS'],
  ['适合场景',        '快速问答、方法论咨询',          'BOM 匹配、项目工作、EPD 数据准备'],
];
---

<BaseLayout title={title} description={description} locale="zh">

  <!-- Hero -->
  <section class="relative">
    <div class="container-page pt-14 md:pt-20 pb-14">
      <div class="mb-10 flex items-baseline justify-between border-b border-rule pb-4">
        <p class="eyebrow">产品 · Cortex Cowork</p>
        <p class="running-footer">{new Date().toISOString().slice(0, 10)}</p>
      </div>
      <h1 class="font-display" style="font-size: clamp(2.4rem, 6vw, 5rem); line-height: 1.05; letter-spacing: -0.025em; font-variation-settings: 'SOFT' 40, 'opsz' 144; font-weight: 400; max-width: 22ch;">
        你的文件。你的机器。<em style="color: var(--shell); font-style: italic; font-variation-settings: 'SOFT' 80;">你的审计记录。</em>
      </h1>
      <p class="mt-10 max-w-2xl font-serif italic" style="font-size: 1.18rem; line-height: 1.55; font-variation-settings: 'opsz' 20; color: var(--ink-mute);">
        Cowork 是专为持续数周的 LCA 项目设计的桌面端 Agent。它从磁盘直接读取 BOM 文件，在本地执行工具，将每一个决策写入随项目文件夹一同走的只追加日志。
      </p>
      <div class="mt-10 flex flex-wrap gap-4">
        <DownloadButtons locale="zh" />
        <a href="#scenarios" class="ticket-ghost">它能处理什么</a>
      </div>
    </div>
  </section>

  <!-- §I 平台 -->
  <section class="border-t border-rule">
    <div class="container-page py-14 md:py-16">
      <div class="eyebrow mb-3">在你的机器上运行</div>
      <p class="max-w-2xl font-serif italic" style="font-size: 0.95rem; line-height: 1.55; font-variation-settings: 'opsz' 16; color: var(--ink-mute);">
        Cortex Cowork 以 macOS 和 Windows 原生二进制发布。<strong style="font-style: normal; color: var(--ink);">Computer Use 仅支持 macOS。</strong>
      </p>
      <div class="mt-8 grid gap-px overflow-hidden border border-rule md:grid-cols-2" style="background: var(--rule);">
        {platforms.map((p) => (
          <div class="p-6" style="background: var(--paper);">
            <div class="flex items-baseline justify-between mb-2">
              <h3 class="font-display" style="font-variation-settings: 'SOFT' 40, 'opsz' 24; font-size: 1.2rem;">{p.name}</h3>
              <span class="font-mono uppercase" style="font-size: 0.62rem; letter-spacing: 0.14em; color: var(--shell);">live</span>
            </div>
            <p class="font-mono" style="font-size: 0.78rem; letter-spacing: 0.04em; color: var(--ink-mute);">{p.detail}</p>
            {p.note && <p class="mt-3 font-serif italic" style="font-size: 0.85rem; line-height: 1.5; font-variation-settings: 'opsz' 14; color: var(--ink-faint);">{p.note}</p>}
          </div>
        ))}
      </div>
    </div>
  </section>

  <!-- §II Dispatch -->
  <section class="border-t border-rule" style="background: var(--paper-rim);">
    <div class="container-page py-20 md:py-24">
      <SectionMark kicker="Cortex Dispatch" numeral="II" title="手机交单。回到桌面接着干。" />
      <div class="max-w-3xl space-y-5 font-serif" style="font-size: 1.05rem; line-height: 1.65; font-variation-settings: 'opsz' 20; color: var(--ink);">
        <p>
          Cortex Dispatch 是桌面端 Cowork 的手机伴侣。在手机上打开 <code style="font-family: 'JetBrains Mono', monospace; font-size: 0.9em;">dispatch.hiq.earth</code>——它以 PWA 形式安装——屏幕只有一个文本框和一个麦克风按钮。打字提交，或按住录音。Dispatch 把任务派给同一账号下登录的桌面 Cowork 实例；Cowork 接到即开跑。
        </p>
        <p>
          语音走 Cloudflare R2：手机直接上传音频，服务端转写，转写文本作为下一条用户消息进入 Cowork 会话。Cowork 窗口不必置于前台，但桌面机器需保持运行且已登录。
        </p>
        <p>
          一个常见场景：采购经理在会议上听到一个供应商名字，口述一句 <em>"把上次 BOM 里 304 不锈钢的行抽出来，按 HiQLCD 1.4 重新匹配一遍。"</em> 四十分钟后回到桌面，匹配后的工作簿已经停在结果表，决策同步写进了 <code style="font-family: 'JetBrains Mono', monospace; font-size: 0.9em;">progress.md</code>。Dispatch 没有替代 Cowork。它只是把触发动作搬出了办公室。
        </p>
        <p>
          Dispatch 是一个遥控器。Cowork 在本地执行的每一道守则——文件写入的项目外路径检查、覆盖率与替代值选择上的"由从业者判断"暂停——无论会话从键盘还是手机发起，都以同样方式触发。SSO 对 Dispatch 的鉴权与桌面端完全一致。
        </p>
      </div>
    </div>
  </section>

  <!-- §III 场景 -->
  <section id="scenarios" class="border-t border-rule">
    <div class="container-page py-20 md:py-28">
      <SectionMark kicker="它能处理什么" numeral="III" title="Chat 跑不了的四种流程。" />

      <div class="mt-14 border-y border-rule">
        {scenarios.map((s) => (
          <article class="border-b border-rule last:border-b-0 py-12">
            <div class="grid items-start gap-6 md:grid-cols-[4rem_minmax(0,_1fr)] md:gap-10">
              <span class="font-display italic" style="color: var(--shell); font-variation-settings: 'SOFT' 40; font-size: clamp(1.5rem, 2.4vw, 1.9rem);">§ {s.numeral}</span>
              <div>
                <h3 class="font-display mb-5" style="font-variation-settings: 'SOFT' 40, 'opsz' 60; font-size: clamp(1.5rem, 2.6vw, 2.1rem); line-height: 1.1; letter-spacing: -0.015em; color: var(--ink);">{s.label}</h3>

                <div class="grid gap-4 mb-6 md:grid-cols-[5rem_minmax(0,_1fr)] md:gap-6 max-w-3xl">
                  <span class="eyebrow" style="color: var(--ink-faint);">之前</span>
                  <p class="font-serif" style="line-height: 1.55; font-variation-settings: 'opsz' 16; color: var(--ink-mute); font-size: 0.95rem;">{s.before}</p>
                  <span class="eyebrow" style="color: var(--shell);">之后</span>
                  <p class="font-serif" style="line-height: 1.55; font-variation-settings: 'opsz' 16; color: var(--ink); font-size: 0.95rem; font-weight: 500;">{s.after}</p>
                </div>

                <p class="font-serif max-w-3xl" style="line-height: 1.6; font-variation-settings: 'opsz' 18; color: var(--ink); font-size: 1rem;">{s.detail}</p>
                <p class="mt-4 font-mono" style="font-size: 0.72rem; letter-spacing: 0.06em; color: var(--ink-faint);">{s.proof}</p>
              </div>
            </div>
          </article>
        ))}
      </div>
    </div>
  </section>

  <!-- §IV BOM 交付物 -->
  <section class="border-t border-rule" style="background: var(--paper-rim);">
    <div class="container-page py-20 md:py-24">
      <SectionMark kicker="BOM 导出" numeral="IV" title="四张表，一个工作簿。" />
      <div class="max-w-3xl space-y-5 font-serif" style="font-size: 1.05rem; line-height: 1.65; font-variation-settings: 'opsz' 20; color: var(--ink);">
        <p>
          BOM 匹配会话的交付物是一个 <code style="font-family: 'JetBrains Mono', monospace; font-size: 0.9em;">.xlsx</code> 工作簿。四张表，顺序如下：
        </p>
        <p>
          <strong style="font-weight: 500;">摘要表</strong> — 覆盖情况一览：匹配率、替代值占比、缺口占比、按物料类别的分类合计。<strong style="font-weight: 500;">结果表</strong> — 主数据表，每行对应 BOM 一行，每列组对应一个数据库，数据集单元格超链接到 <code style="font-family: 'JetBrains Mono', monospace; font-size: 0.9em;">hiqlcd.com</code> 的源记录。<strong style="font-weight: 500;">缺口表</strong> — 列出未匹配与替代值行，注明搜索过的数据库与建议的下一步操作。<strong style="font-weight: 500;">方法论表</strong> — 运行参数和固定说明，让审核人能够复现这次工作（系统模型、IPCC AR6 下的 GWP100、替代值定义、需从业者审查的要求）。
        </p>
        <p>
          字段级讲解——每一列、每一个状态图标、每一条 <code style="font-family: 'JetBrains Mono', monospace; font-size: 0.9em;">Δ%</code> 规则——见 <a href="/zh/solutions/bom-matching" style="color: var(--shell);">/zh/solutions/bom-matching</a>。本页告诉你 Cowork 能产出这份工作簿；那一页告诉你工作簿里究竟有什么。
        </p>
      </div>
    </div>
  </section>

  <!-- §V Schedules -->
  <section class="border-t border-rule">
    <div class="container-page py-20 md:py-24">
      <SectionMark kicker="定时任务" numeral="V" title="工作流上时钟。" />
      <div class="max-w-3xl space-y-5 font-serif" style="font-size: 1.05rem; line-height: 1.65; font-variation-settings: 'opsz' 20; color: var(--ink);">
        <p>
          Cowork 跑工作流。定时任务把同样的工作流挂到时钟上。每日、每周，或一条完整的 cron 表达式——Cowork 触发运行、记录结果、把下一步推到项目时间线上。
        </p>
        <p>
          三种形态，按项目绑定程度递增：
        </p>
      </div>

      <div class="mt-8 grid gap-px overflow-hidden border border-rule md:grid-cols-3 max-w-5xl" style="background: var(--rule);">
        {scheduleShapes.map((s) => (
          <article class="p-6 md:p-7" style="background: var(--paper);">
            <h3 class="font-display mb-3" style="font-variation-settings: 'SOFT' 40, 'opsz' 28; font-size: 1.1rem; color: var(--shell);">{s.label}</h3>
            <p class="font-serif" style="line-height: 1.6; font-variation-settings: 'opsz' 18; color: var(--ink); font-size: 0.95rem;">{s.body}</p>
          </article>
        ))}
      </div>

      <div class="mt-10 max-w-3xl space-y-5 font-serif" style="font-size: 1.05rem; line-height: 1.65; font-variation-settings: 'opsz' 20; color: var(--ink);">
        <p>
          运行历史以图标停在项目时间线上：成功、失败、运行中。点开任一条，读那次运行的 Agent 日志；点关联的任务或定时任务，跳回源头。
        </p>
        <p>
          Cowork 常落入的一种模式：项目的 <code style="font-family: 'JetBrains Mono', monospace; font-size: 0.9em;">tasks.md</code> 列出一条循环动作。Cowork 在项目打开时读到它，自动创建一条任务绑定型定时任务——从业者看到 cron 建议，确认即可。
        </p>
      </div>

      <Asterism />

      <blockquote class="mx-auto max-w-3xl text-center">
        <p class="pullquote">
          场景跑一遍工作流。定时任务<em style="color: var(--shell); font-style: italic;">让它一直跑下去。</em>
        </p>
      </blockquote>
    </div>
  </section>

  <!-- §VI 数据隐私 -->
  <section class="border-t border-rule" style="background: var(--paper-rim);">
    <div class="container-page py-20 md:py-24">
      <SectionMark kicker="数据留在你的机器上" numeral="VI" title="本地目录。本地会话。本地审计记录。" />
      <div class="max-w-3xl space-y-5 font-serif" style="font-size: 1.05rem; line-height: 1.65; font-variation-settings: 'opsz' 20; color: var(--ink);">
        <p>
          Cowork 会话不上传。BOM 文件在会话的本地上下文中处理，不在服务端持久化。
        </p>
        <p>
          启动时，4 个 LCA 目录下载到本地存储——HiQLCD 1.4、Ecoinvent 3.12、CarbonMinds 2.0、Agri-footprint 7.0——并通过验证同步保持更新。目录检索在本地对已下载文件运行，目录查询不发起网络请求。下载完成后，目录支持离线使用。
        </p>
        <p>
          <strong style="font-weight: 500;">项目外路径检查</strong>在每次文件写入和 Shell 命令时触发。文件边界检查在执行前拦截任何路径落在项目文件夹外的操作。用户会看到确认卡片：具体路径、一句话原因，以及三个选项——仅此一次允许 / 始终允许此目录 / 拒绝。该检查无论会话模式如何均会触发，Agent 无法绕过。
        </p>
        <p>
          项目的 L5 审计日志（<code style="font-family: 'JetBrains Mono', monospace; font-size: 0.9em;">progress.md</code>，详见下方）和 L6 项目记忆文件存储在用户本地文件系统，与项目数据同目录。它们可以提交到版本控制系统。
        </p>
      </div>
    </div>
  </section>

  <!-- §VII 六层记忆 -->
  <section class="border-t border-rule">
    <div class="container-page py-20 md:py-24">
      <SectionMark kicker="六层记忆" numeral="VII" title="每次 Cowork 会话开始时，六层上下文已就位。" />

      <div class="mt-10 overflow-hidden border border-rule" style="background: var(--paper);">
        <table class="w-full" style="border-collapse: collapse;">
          <thead>
            <tr style="background: var(--paper-rim);">
              <th class="eyebrow text-left p-4 border-b border-rule">层级</th>
              <th class="eyebrow text-left p-4 border-b border-rule">携带内容</th>
              <th class="eyebrow text-left p-4 border-b border-rule">写入方</th>
            </tr>
          </thead>
          <tbody class="font-serif" style="font-size: 0.9rem; line-height: 1.5; font-variation-settings: 'opsz' 16;">
            {memoryLayers.map(([layer, what, who]) => (
              <tr>
                <td class="p-4 border-b border-rule" style="color: var(--shell); font-weight: 500;">{layer}</td>
                <td class="p-4 border-b border-rule" style="color: var(--ink);">{what}</td>
                <td class="p-4 border-b border-rule" style="color: var(--ink-mute);">{who}</td>
              </tr>
            ))}
          </tbody>
        </table>
      </div>

      <div class="mt-10 max-w-3xl space-y-5 font-serif" style="font-size: 1.05rem; line-height: 1.65; font-variation-settings: 'opsz' 20; color: var(--ink);">
        <p>
          <strong style="font-weight: 500;">L5 是审计记录。</strong> Agent 无法直接编辑 <code style="font-family: 'JetBrains Mono', monospace; font-size: 0.9em;">progress.md</code>。只有专用的追加工具被允许写入——在系统层面强制执行，而非依赖约定。每条记录包含类型（六种之一：<code style="font-family: 'JetBrains Mono', monospace; font-size: 0.85em;">decision</code>、<code style="font-family: 'JetBrains Mono', monospace; font-size: 0.85em;">data_gap</code>、<code style="font-family: 'JetBrains Mono', monospace; font-size: 0.85em;">assumption</code>、<code style="font-family: 'JetBrains Mono', monospace; font-size: 0.85em;">verification</code>、<code style="font-family: 'JetBrains Mono', monospace; font-size: 0.85em;">challenge</code>、<code style="font-family: 'JetBrains Mono', monospace; font-size: 0.85em;">milestone</code>）、主题、带时间戳的内容，以及可选的下一步说明。
        </p>
        <p>
          一次 BOM 匹配会话会产生以下记录：<code style="font-family: 'JetBrains Mono', monospace; font-size: 0.85em;">[decision]</code> — 选择了哪个数据集及原因；<code style="font-family: 'JetBrains Mono', monospace; font-size: 0.85em;">[data_gap]</code> — 无目录匹配的物料；<code style="font-family: 'JetBrains Mono', monospace; font-size: 0.85em;">[assumption]</code> — 使用的替代值及其偏差方向。
        </p>
      </div>

      <Asterism />

      <blockquote class="mx-auto max-w-3xl text-center">
        <p class="pullquote">
          审计员问"这个 GWP100 值是怎么来的"，读的是 <em style="color: var(--shell); font-style: italic;">progress.md</em>。
        </p>
      </blockquote>
    </div>
  </section>

  <!-- §VIII Project wiki -->
  <section class="border-t border-rule" style="background: var(--paper-rim);">
    <div class="container-page py-20 md:py-24">
      <SectionMark kicker="项目 wiki" numeral="VIII" title="为这个项目所知的一切，建一张实体图。" />
      <div class="max-w-3xl space-y-5 font-serif" style="font-size: 1.05rem; line-height: 1.65; font-variation-settings: 'opsz' 20; color: var(--ink);">
        <p>
          每个 Cowork 项目都带一份 wiki：一个本地数据库，存放结构化的<strong style="font-weight: 500;">条目</strong>——类型包括 <code style="font-family: 'JetBrains Mono', monospace; font-size: 0.88em;">person</code>、<code style="font-family: 'JetBrains Mono', monospace; font-size: 0.88em;">organization</code>、<code style="font-family: 'JetBrains Mono', monospace; font-size: 0.88em;">dataset</code>、<code style="font-family: 'JetBrains Mono', monospace; font-size: 0.88em;">artifact</code>、<code style="font-family: 'JetBrains Mono', monospace; font-size: 0.88em;">concept</code>、<code style="font-family: 'JetBrains Mono', monospace; font-size: 0.88em;">event</code> 等——由类型化的<strong style="font-weight: 500;">关系</strong>相连（<code style="font-family: 'JetBrains Mono', monospace; font-size: 0.88em;">uses</code>、<code style="font-family: 'JetBrains Mono', monospace; font-size: 0.88em;">cites</code>、<code style="font-family: 'JetBrains Mono', monospace; font-size: 0.88em;">part_of</code>、<code style="font-family: 'JetBrains Mono', monospace; font-size: 0.88em;">created_by</code>、<code style="font-family: 'JetBrains Mono', monospace; font-size: 0.88em;">describes</code>、<code style="font-family: 'JetBrains Mono', monospace; font-size: 0.88em;">contradicts</code>、<code style="font-family: 'JetBrains Mono', monospace; font-size: 0.88em;">supersedes</code> 等）。这是一张从业者可以查询的图。
        </p>
        <p>
          桌面 UI 提供两种视图。<strong style="font-weight: 500;">列表</strong>展示所有条目，支持搜索与筛选——打开一条，在 modal 里编辑字段，保存。<strong style="font-weight: 500;">图谱</strong>把同一份数据渲染成力导布局图：节点按类型上色浮动，边显示哪些物料共用了供应商、哪些数据集反复被选中匹配哪些行、哪些决策引用了哪些来源。拖动一个节点，检视它的邻域。
        </p>
        <p>
          一个真实例子。打开 <code style="font-family: 'JetBrains Mono', monospace; font-size: 0.9em;">Hangzhou Metal Co.</code>——一个 <code style="font-family: 'JetBrains Mono', monospace; font-size: 0.88em;">organization</code> 节点。<code style="font-family: 'JetBrains Mono', monospace; font-size: 0.88em;">created_by</code> 边指向 <code style="font-family: 'JetBrains Mono', monospace; font-size: 0.9em;">304 stainless steel coil</code> 和 <code style="font-family: 'JetBrains Mono', monospace; font-size: 0.9em;">316L</code>，是它供给的 artifacts。每个 artifact <code style="font-family: 'JetBrains Mono', monospace; font-size: 0.88em;">cites</code> 一个被匹配的 ecoinvent 数据集，并 <code style="font-family: 'JetBrains Mono', monospace; font-size: 0.88em;">describes</code> 一条 <code style="font-family: 'JetBrains Mono', monospace; font-size: 0.88em;">concept</code> 条目——里面记录着从业者为那一行做出的 APOS-over-cut-off 决策。下个月新项目里同一个供应商再次出现时，图已经知道是谁、是什么。
        </p>
        <p>
          wiki 与项目记忆的另外两种形态并列，做它们做不到的事。<code style="font-family: 'JetBrains Mono', monospace; font-size: 0.9em;">progress.md</code>（L5）是只追加的时间线，回答 <em>什么时间我们做了什么决定</em>。L6 的项目记忆文件（<code style="font-family: 'JetBrains Mono', monospace; font-size: 0.9em;">Cortex.md</code> 及同侪）承载 <em>这个项目学到了什么</em>——已核查的数据集、反复使用的替代值、供应商专属说明。wiki 是结构化的，回答 <em>这个项目知道的事物之间如何关联</em>。审计走 L5。约定查询走 L6。知识穿行走 wiki。
        </p>
      </div>
    </div>
  </section>

  <!-- §IX 技能 -->
  <section class="border-t border-rule">
    <div class="container-page py-20 md:py-24">
      <SectionMark kicker="技能" numeral="IX" title="平台从实际工作里学。" />
      <div class="max-w-3xl space-y-5 font-serif" style="font-size: 1.05rem; line-height: 1.65; font-variation-settings: 'opsz' 20; color: var(--ink);">
        <p>
          Cowork 内置技能随应用出厂即加载。平台从两个来源持续扩充：共享市场和从业者自身的会话。
        </p>
        <p>
          <strong style="font-weight: 500;">内置技能</strong>随应用发布：<code style="font-family: 'JetBrains Mono', monospace; font-size: 0.88em;">bom-analysis</code>、<code style="font-family: 'JetBrains Mono', monospace; font-size: 0.88em;">querying-lca-datasets</code>、<code style="font-family: 'JetBrains Mono', monospace; font-size: 0.88em;">scope3-guidance</code>、<code style="font-family: 'JetBrains Mono', monospace; font-size: 0.88em;">en-15804-indicators</code>。从第一次会话起即可使用。
        </p>
        <p>
          <strong style="font-weight: 500;">市场技能</strong>由从业者贡献、经 HiQ-AI 审核后向所有用户开放。Cowork 在启动时检查更新并静默下载。技能缓存后支持离线使用。
        </p>
        <p>
          <strong style="font-weight: 500;">自定义技能</strong>从从业者自身的工作中积累。观察流水线在每次会话结束后对其建立索引。当两次或更多会话呈现相同的工作流形态时，后台进程读取实际会话记录并生成可复用技能。无需用户操作。技能出现在近期学习面板，并在下一次类似任务时自动加载。
        </p>
        <p>
          任何自定义技能均可贡献到共享市场。离开本机前会进行个人信息扫描。扫描通过后，向 HiQ-AI 开启拉取请求供审核。合并后，下次启动时该技能对所有用户可用。
        </p>
        <p class="font-display italic" style="color: var(--shell); font-size: 1.15rem; line-height: 1.4;">
          平台对 LCA 的理解不归厂家所有。它从从业者的真实工作里长出来。
        </p>
        <p>
          <a href="/zh/skills" class="smallcaps" style="color: var(--shell);">查看技能详情 →</a>
        </p>
      </div>
    </div>
  </section>

  <!-- §X Computer Use -->
  <section class="border-t border-rule" style="background: var(--paper-rim);">
    <div class="container-page py-20 md:py-24">
      <SectionMark kicker="Computer Use" numeral="X" title="仅限 macOS — 直接控制已安装应用的 GUI。" />
      <div class="max-w-3xl space-y-5 font-serif" style="font-size: 1.05rem; line-height: 1.65; font-variation-settings: 'opsz' 20; color: var(--ink);">
        <p>
          这是 Cowork 成为 Agent 而非脚本的原因。它可以驱动机器上任何已安装的应用——包括没有 API、没有插件生态的软件。
        </p>
        <p>
          启用 Computer Use 后，Cowork 获得 21 个 MCP 工具用于直接 GUI 交互：截图、缩放、点击（左键、右键、双击、三击、中键）、输入文字、按键、滚动、拖拽、启动应用、剪贴板读写等。Agent 可以驱动任何已安装的桌面应用——openLCA 对话框、SimaPro 结果窗口、旧版 ERP、用户 PATH 中的任何程序——无需命令行接口。每次操作前，用户审批 Agent 可控制哪些应用。主窗口缩至侧边面板；可见遮罩显示"Claude is using your computer."随时按 Escape 中断。
        </p>
        <p class="font-serif italic" style="color: var(--ink-mute);">
          Computer Use 不支持 Windows。
        </p>
      </div>
    </div>
  </section>

  <!-- §XI 对比 -->
  <section class="border-t border-rule">
    <div class="container-page py-20 md:py-24">
      <SectionMark kicker="Cowork 与 Chat" numeral="XI" title="Cortex Cowork 跑流程。在你的机器上。在你的项目文件夹里。" />

      <div class="mt-10 overflow-hidden rounded-sm border border-rule" style="background: var(--paper);">
        <table class="w-full" style="border-collapse: collapse;">
          <thead>
            <tr style="background: var(--paper-rim);">
              <th class="eyebrow text-left p-4 border-b border-rule"></th>
              <th class="eyebrow text-left p-4 border-b border-rule">Cortex Chat</th>
              <th class="eyebrow text-left p-4 border-b border-rule" style="color: var(--shell);">Cortex Cowork</th>
            </tr>
          </thead>
          <tbody class="font-serif" style="font-size: 0.92rem; line-height: 1.5; font-variation-settings: 'opsz' 16;">
            {compare.map(([label, chat, cowork]) => (
              <tr>
                <td class="p-4 border-b border-rule" style="color: var(--ink-mute); font-weight: 500;">{label}</td>
                <td class="p-4 border-b border-rule" style="color: var(--ink-mute);">{chat}</td>
                <td class="p-4 border-b border-rule" style="color: var(--ink);">{cowork}</td>
              </tr>
            ))}
          </tbody>
        </table>
      </div>

      <p class="mt-8">
        <a href="/zh/product/chat" class="smallcaps" style="color: var(--ink-mute);">查看 Cortex Chat →</a>
      </p>
    </div>
  </section>

  <!-- 结语 -->
  <section class="border-t border-rule">
    <div class="container-prose py-24 text-center">
      <h2 class="font-display" style="font-variation-settings: 'SOFT' 40, 'opsz' 96; font-size: clamp(2rem, 5vw, 3rem); line-height: 1.05; letter-spacing: -0.02em;">
        安装一次。从上次停下的地方接着跑。
      </h2>
      <p class="mx-auto mt-8 max-w-xl font-serif italic" style="font-size: 1.05rem; line-height: 1.6; font-variation-settings: 'opsz' 20; color: var(--ink-mute);">
        Cowork 在重启之间保留项目上下文：已做的决策、已记录的替代值选择、已匹配的行。六层记忆在磁盘上，不在云端会话里。项目下次打开时，Agent 从上次会话结束的地方开始。
      </p>
      <div class="mt-10 flex flex-wrap justify-center gap-4">
        <DownloadButtons locale="zh" />
        <a href="mailto:info@hiqlcd.com" class="ticket-ghost">有疑问 · info@hiqlcd.com</a>
      </div>
      <div class="mt-8 flex flex-wrap justify-center gap-x-8 gap-y-3 text-sm font-serif" style="color: var(--ink-mute); font-variation-settings: 'opsz' 14;">
        <a href="/zh/standards" style="color: var(--ink-mute);">标准对齐——ISO 14067、CBAM、EF 方法学</a>
        <a href="/zh/security" style="color: var(--ink-mute);">数据不离开你的设备</a>
        <a href="/zh/solutions/bom-matching" style="color: var(--ink-mute);">BOM 匹配流程说明</a>
      </div>
    </div>
  </section>
</BaseLayout>
